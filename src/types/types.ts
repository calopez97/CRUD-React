export interface User{
    user:{
        firstName: string;
        id?: string;
        lastName: string;
        picture?: string;
        title?: string;
    }
}


export interface translateTitle{
    ms: string;
    miss: string;
    mr: string;
}